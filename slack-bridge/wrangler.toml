name = "routines-slack-bridge"
main = "src/index.js"
compatibility_date = "2026-05-21"

# Set this to the routine's API trigger URL after creating the routine.
# Set via `wrangler secret put` (do NOT commit secrets here):
#   SLACK_SIGNING_SECRET    Slack app signing secret (1Password)
#   ROUTINE_BEARER_TOKEN    Bearer token from the routine's API trigger config
[vars]
ROUTINE_ENDPOINT = "https://REPLACE_ME.routines.anthropic.com/trigger"
